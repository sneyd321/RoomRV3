mutation updateRentDiscounts($houseId: Int!, $rentDiscounts: [RentDiscountInput!]!){
   updateRentDiscounts(houseId: $houseId, rentDiscounts: $rentDiscounts) {
        name,
        amount,
        details {
            detail
        }   
  }
}