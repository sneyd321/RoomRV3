mutation updateUtilities($houseId: Int!, $utilities: [UtilityInput!]!) {
   updateUtilities(houseId: $houseId, utilities: $utilities) {
        name,
        responsibility,
        details {
            detail
        }      
    }
}